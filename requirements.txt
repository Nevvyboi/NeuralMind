# GroundZero AI - Requirements
# ============================

# State-of-the-Art NLP
spacy>=3.5.0

# Note: After installing, download the English model:
#   python -m spacy download en_core_web_sm
#
# For better accuracy (recommended):
#   python -m spacy download en_core_web_md
#
# For best accuracy (requires more RAM):
#   python -m spacy download en_core_web_lg

# Optional: Higher-level NLP utilities
# textacy>=0.12.0

# The rest uses Python standard library:
# - sqlite3 (built-in)
# - json (built-in)
# - urllib (built-in)
# - http.server (built-in)
# - dataclasses (built-in, Python 3.7+)
# - typing (built-in)
# - pathlib (built-in)
# - re (built-in)